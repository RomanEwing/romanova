Romanova — /assets/

Drop the ManaPool QR images here (referenced by the ManaPool block in index.html):

  manapool-store-qr.png      QR to the Romanova ManaPool seller page  [ROMAN: store URL]
  manapool-referral-qr.png   QR to Roman's ManaPool referral signup link

If a file is missing, the page hides that QR automatically (onerror), so the links
still work without the images. PNG or SVG is fine; keep them roughly square.
